// Bucle For
for (let indexFor = 1; indexFor <= 10; indexFor++) {
    console.log('Iteracion For ' + indexFor)   
}

// Bucle While
let indexWhile = 1;
while (indexWhile <= 10){
    console.log('Iteracion While ' + indexWhile);
    indexWhile += 1;
}